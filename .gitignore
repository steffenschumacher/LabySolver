# Build artifacts (all binaries are compiled from tests/*.cpp — see
# docs/BUILD_AND_TEST.md; never commit them, always rebuild locally).
sketch/bin/
build/
*.o
*.obj
*.a
*.so
*.dylib
*.exe

# Debugger artifacts
*.dSYM/
core

# Editor/OS cruft
.DS_Store
*.swp
*~
.vscode/
.idea/

# Original LabyMania material supplied for local reference only.  The project
# contains independently written rules/fixtures derived from it, never these
# copyrighted source/assets or archive metadata.
LabyMania/
LabyMania.zip
__MACOSX/

# CUDA build artifacts (once a real .cu kernel is added)
*.ptx
*.cubin
*.fatbin
